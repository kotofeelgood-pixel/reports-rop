/**
 * Унифицированная точка доступа к Bitrix24 JS API через bx24-api.
 * 
 * Теперь используем пакет bx24-api, который предоставляет Promise-based интерфейс
 * и автоматически инициализирует Bitrix24 библиотеку.
 */

import BX24 from 'bx24-api'

/**
 * Тип для совместимости со старым кодом (не используется напрямую, но может быть полезен)
 */
export type B24Like = typeof BX24

/**
 * Получить экземпляр BX24 API из bx24-api пакета.
 * Пакет автоматически инициализирует библиотеку при первом вызове.
 */
export const useB24 = async (): Promise<typeof BX24> => {
  // bx24-api автоматически инициализирует библиотеку при первом вызове
  // Просто возвращаем экземпляр BX24
  return BX24
}

